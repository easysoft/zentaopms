#!/usr/bin/env php
<?php
include dirname(dirname(dirname(__FILE__))) . '/lib/init.php';
include dirname(dirname(dirname(__FILE__))) . '/class/stakeholder.class.php';
su('admin');

/**

title=测试 stakeholderModel->getListByType();
cid=1
pid=1

*/
global $tester;
$stakeholder = $tester->loadModel('stakeholder');

var_dump($stakeholder->getListByType());die;
r($stakeholder->getListByTypeTest()) && p() && e();
