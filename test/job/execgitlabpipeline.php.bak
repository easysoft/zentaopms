#!/usr/bin/env php
<?php
include dirname(dirname(dirname(__FILE__))) . '/lib/init.php';
include dirname(dirname(dirname(__FILE__))) . '/class/job.class.php';
su('admin');

/**

title=jobModel->execGitlabPipeline();
cid=1
pid=1


*/

$jobID = 2;

$job = new jobTest();
r($job->execGitlabPipelineTest($jobID)) && p('status')&& e('create_fail');     // 测试执行jekins pipeline情况
//system("./ztest init");
